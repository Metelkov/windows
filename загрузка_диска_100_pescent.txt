загрузка диска 100%

Dism /Online /Cleanup-Image /RestoreHealth

зайти в «Редактор реестра»
«Найти» - «PrefetchParameters» и запустить поиск
В папке реестра «PrefetchParameters» потребуется изменить значение параметров 
«Enable Prefetech» и «EnableSuperFetch», проставив поочерёдно в каждом из них 
в графе «Значение» цифру 0 и подтвердив нажатием кнопки «ОК».
Дальше потребуется зайти в службы, и отключить Superfetch, проследовав по уже 
известному пути: «Свойства Superfetch», состояние – «Остановить», тип запуска – «Отключена», «Применить».

KtmRm для координатора распределенных транзакций
Snupchat Superfetch SysMain
Агент политики IPsec PolicyAgent
Клиент отслеживания изменившихся связей
Служба политики диагностики DPS
